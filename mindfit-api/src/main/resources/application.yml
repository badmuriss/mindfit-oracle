spring:
  application:
    name: Mindfit API
  data:
    mongodb:
      uri: mongodb://localhost:27017/mindfit
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-openai-api-key}
      chat:
        options:
          model: gpt-3.5-turbo

server:
  servlet:
    context-path: /api/v1

app:
  jwt:
    secret: mySecretKeyForDevelopmentPleaseChangeInProduction
    expiration-ms: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.mindfit: DEBUG

---
spring:
  config:
    activate:
      on-profile: production
  data:
    mongodb:
      uri: ${MONGO_URI}
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000

logging:
  level:
    com.mindfit: INFO